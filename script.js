let first = "firstString";
let second = " and secondString";

console.log (`concatenation is joining the two strings: 
${first + second}`);

/*join the 2 strings by putting the name of the string in the curley brackets preceded by the dollor sign: `${fisrt + second}` */